// Stampare a schermo un messaggio all’interno di un h1, utilizzando i data.

let root = new Vue(
    {
        el: "#root",
        data: {
            message: "Hello Everybody"
        }
    }
);